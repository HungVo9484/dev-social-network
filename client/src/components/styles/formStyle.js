import { css } from "styled-components";

export const formGroup = css`
  margin: 1.2rem 0;
`;

export const formText = css`
  display: block;
  margin-top: 0.3rem;
  color: #888;
`;

export const inputFormStyle = css`
  display: block;
  width: 100%;
  padding: 0.4rem;
  font-size: 1.2rem;
  border: 1px solid #ccc;
  @media (max-width: 700px) {
    font-size: 1rem;
  }
`;

export const submitFormStyle = css`

`;

